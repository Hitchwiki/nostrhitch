services:
  nostrhitch:
    build: .
    container_name: nostrhitch-daemon
    restart: unless-stopped
    volumes:
      - ./config.json:/root/config.json:ro
      - ./logs:/root/logs
      - ./hitchmap-dumps:/root/hitchmap-dumps
    environment:
      - TZ=UTC
    command: ["./nostrhitch-daemon"]
